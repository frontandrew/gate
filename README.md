# GATE - Gate Access Control System

Система автоматической авторизации транспорта при въезде на охраняемую территорию с использованием распознавания номерных знаков.

## 🏗️ Архитектура

- **Backend**: Go (Clean Architecture)
- **Database**: PostgreSQL 16
- **Cache**: Redis 7
- **ML Service**: Python (FastAPI) + EasyOCR/Tesseract
- **Frontend**: Vue 3 + Pinia + TypeScript
- **Infrastructure**: Docker Compose

### Ключевой принцип архитектуры

**Пользователь - центральная сущность системы**. Автомобильный номер - это способ аутентификации пользователя, а не самостоятельная сущность.

**Логика проверки доступа:**
```
Номер авто → Автомобиль → Владелец (Пользователь) → Активный пропуск → Решение о доступе
```

См. подробнее: [ARCHITECTURE.md](./ARCHITECTURE.md)

## 📋 Требования

- Docker & Docker Compose
- Go 1.22+ (для локальной разработки)
- Make (опционально, но рекомендуется)
- PostgreSQL migrate tool (для миграций)

## 🚀 Быстрый старт

### 1. Клонирование проекта

```bash
git clone https://github.com/frontandrew/gate.git
cd gate
```

### 2. Настройка окружения

Скопируйте файл с примером переменных окружения:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл при необходимости (по умолчанию настройки готовы к использованию).

### 3. Запуск инфраструктуры

Запустите PostgreSQL и Redis:

```bash
make docker-up
```

Или без Make:

```bash
docker-compose -f docker/docker-compose.yml up -d
```

### 4. Применение миграций

```bash
make migrate-up
```

Или напрямую:

```bash
migrate -path migrations -database "postgresql://gate_user:gate_password@localhost:5432/gate_db?sslmode=disable" up
```

### 5. Запуск приложения

```bash
make run
```

Или:

```bash
go run cmd/api/main.go
```

## 📦 Доступные команды Make

```bash
make help              # Показать все доступные команды
make install           # Установить Go зависимости
make docker-up         # Запустить Docker сервисы
make docker-down       # Остановить Docker сервисы
make docker-logs       # Показать логи Docker
make migrate-up        # Применить миграции БД
make migrate-down      # Откатить миграции БД
make migrate-create    # Создать новую миграцию
make run               # Запустить API сервер
make build             # Собрать бинарник
make test              # Запустить тесты
make test-coverage     # Тесты с отчетом покрытия
make lint              # Запустить линтер
make seed              # Заполнить БД тестовыми данными
make clean             # Очистить артефакты сборки
make dev               # Запустить окружение для разработки
```

## 🗂️ Структура проекта

```
gate/
├── cmd/                      # Точки входа приложения
│   ├── api/                 # Основной API сервер
│   └── migrate/             # CLI для миграций
├── internal/                # Приватный код приложения
│   ├── domain/              # Бизнес-сущности и правила
│   ├── usecase/             # Use cases (бизнес-логика)
│   ├── repository/          # Data access layer
│   ├── delivery/            # HTTP handlers
│   ├── infrastructure/      # Внешние сервисы (ML, storage)
│   └── pkg/                 # Утилиты и вспомогательные пакеты
├── migrations/              # SQL миграции
├── docker/                  # Docker конфигурация
├── scripts/                 # Утилиты и скрипты
├── .env.example            # Пример переменных окружения
├── go.mod                  # Go модуль
├── Makefile                # Команды для разработки
└── README.md               # Этот файл
```

## 🔧 Конфигурация

Основные переменные окружения находятся в файле `.env`:

```bash
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=gate_user
DB_PASSWORD=gate_password
DB_NAME=gate_db

# Server
SERVER_PORT=8080

# JWT
JWT_SECRET=your-secret-key
JWT_ACCESS_EXPIRY=3600

# ML Service
ML_SERVICE_URL=http://localhost:8001
ML_MIN_CONFIDENCE=0.7
```

## 🛢️ База данных

### Создание новой миграции

```bash
make migrate-create name=add_users_table
```

### Применение миграций

```bash
make migrate-up
```

### Откат миграций

```bash
make migrate-down
```

## 📊 API Endpoints

### Итерация 1 (MVP)

- `POST /api/v1/access/check` - Проверка доступа и распознавание номера
- `POST /api/v1/access/grant` - Команда на открытие ворот
- `GET /api/v1/access/logs` - История проездов

### Полная документация API

После запуска сервера, документация API будет доступна по адресу:
- Swagger UI: `http://localhost:8080/swagger/index.html`

## 🧪 Тестирование

### Запуск всех тестов

```bash
make test
```

### Тесты с покрытием

```bash
make test-coverage
```

Отчет будет сохранен в `coverage.html`.

## 🐳 Docker

### Запуск всех сервисов

```bash
docker-compose -f docker/docker-compose.yml up -d
```

### Просмотр логов

```bash
docker-compose -f docker/docker-compose.yml logs -f
```

### Остановка сервисов

```bash
docker-compose -f docker/docker-compose.yml down
```

## 📝 Разработка

### Порядок разработки (по итерациям)

#### Итерация 0: Setup ✅
- [x] Структура проекта
- [x] Docker инфраструктура
- [x] Обновленная миграция БД (users, vehicles, passes, access_logs)
- [x] Makefile
- [x] Архитектурная документация

#### Итерация 1: MVP (следующая)
**Все основные сущности в одной итерации:**
- [ ] Domain models (User, Vehicle, Pass, AccessLog)
- [ ] Repository layer для всех сущностей
- [ ] ML сервис (Python + EasyOCR)
- [ ] ML клиент (Go)
- [ ] Use cases с правильной логикой (User-центрированная)
- [ ] HTTP handlers и API
- [ ] Main application
- [ ] Тестовые данные и проверка

**Ключевое отличие**: Создаем сразу пользователей, автомобили привязаны к пользователям, проверка доступа идет через владельца.

#### Итерация 2: JWT аутентификация и Frontend
- [ ] JWT токены (уже готова БД структура)
- [ ] Auth middleware
- [ ] Vue 3 + Pinia frontend
- [ ] Личный кабинет

#### Итерация 3: Временные пропуска
- [ ] Логика временных ограничений
- [ ] RBAC middleware
- [ ] Cron job для истечения
- [ ] UI управления

#### Итерация 4: История и аналитика
- [ ] Pass history (миграция отдельная)
- [ ] Статистика по пользователям
- [ ] Отчеты и графики

### Установка дополнительных инструментов

```bash
# golang-migrate
go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest

# golangci-lint
go install github.com/golangci-lint/golangci-lint/cmd/golangci-lint@latest

# swag (Swagger documentation)
go install github.com/swaggo/swag/cmd/swag@latest
```

## 🤝 Участие в разработке

1. Создайте ветку для новой функциональности: `git checkout -b feature/your-feature`
2. Внесите изменения и закоммитьте: `git commit -am 'Add some feature'`
3. Запушьте в ветку: `git push origin feature/your-feature`
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 👥 Авторы

- Andrew Front ([@frontandrew](https://github.com/frontandrew))

## 📞 Контакты

По вопросам разработки обращайтесь через Issues на GitHub.

---

**Статус проекта**: 🚧 В разработке (Итерация 0 завершена, Итерация 1 в процессе)
